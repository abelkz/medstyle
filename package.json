{
  "name": "galium",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "render:build": "cd server && npm install && npx prisma generate && npm run build && cd ../client && npm install && npm run build",
    "render:start": "cd server && npx prisma db push --accept-data-loss && npx tsx prisma/seed.ts && node dist/index.js"
  }
}
